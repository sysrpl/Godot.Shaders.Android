# Godot 4+ specific ignores
.godot/
.import/
export.cfg
export_credentials.cfg
data_*/
mono_crash.*.json
helpers.sh
*.import
*.uid
*.mp4

# .NET
bin/
obj/

# Build output and Wi-Fi adb cache
builds/
.wifi_device_ip

# Android Gradle build tree (regenerated from export templates; large, not source)
android/build/

# Release signing keystore - the password lives in export_credentials.cfg (already ignored
# above); the keystore file itself must never be committed either.
keystores/
